doctype html
html
  head
    title えくすたーなるとぅいったー
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    link(rel='stylesheet', href='/stylesheets/style.css')
    link(rel="stylesheet",
    href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css",
    integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm",
    crossorigin="anonymous")
  body
    nav.navbar.navbar-dark.bg-dark.sticky-top.pt-4.pb-4
      a(href="/").navbar-brand.nav-link えくすたーなるとぅいったー
      if user
        form(method="post" action="/tweets/result").form-inline
          input(type="text" name="word" placeholder="えくすたーなる！！" autocomplete="off").form-control.px-5
          input(type="hidden" name="_csrf" value!=csrfToken)
          button(type="submit").btn.btn-outline-info.mx-2 検索する
        a(href="/tweets/favorite").navbar-brand.nav-link お気に入りへ
        a(href=`https://twitter.com/${user.username}`).navbar-brand.nav-link 自分のツイッターへ
        a(href="/logout").navbar-brand.nav-link #{user.username} をログアウト
      else
        a(href="/login").navbar-brand.nav-link ログイン
    div.container
      block content
    script(src="/javascripts/bundle.js")